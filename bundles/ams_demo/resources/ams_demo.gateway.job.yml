resources:
  jobs:
    ams_demo_gateway:
      name: ams_demo_gateway

      max_concurrent_runs: 1

      continuous:
        pause_status: UNPAUSED
        task_retry_mode: ON_FAILURE

      tasks:
        - task_key: capture_to_staging
          notebook_task:
            notebook_path: ../../../src/data_platform/lakeflow/ams_demo/gateway/gateway_simulator.ipynb