resources:
  pipelines:
    ams_streaming_etl:
      name: ams_streaming_etl

      catalog: ${var.catalog}
      schema: ${var.schema}

      serverless: true

      libraries:
        - glob:
            include: ../../../src/data_platform/lakeflow/ams_demo/streaming/**