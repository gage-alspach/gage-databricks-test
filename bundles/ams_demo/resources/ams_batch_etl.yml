resources:
  pipelines:
    ams_batch_etl:
      name: ams_batch_etl

      catalog: ${var.catalog}
      schema: ${var.schema}

      serverless: true

      libraries:
        - glob:
            include: ../../../src/data_platform/lakeflow/ams_demo/batch/transformations/**