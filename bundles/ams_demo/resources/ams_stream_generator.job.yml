resources:
  jobs:
    ams_stream_generator:
      name: ams_stream_generator

      continuous:
        pause_status: UNPAUSED
        task_retry_mode: ON_FAILURE

      max_concurrent_runs: 1

      tasks:
        - task_key: generate_files
          notebook_task:
            notebook_path: ../../../src/data_platform/lakeflow/ams_demo/generator/file_generator.ipynb