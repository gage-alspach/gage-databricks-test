resources:
  jobs:
    streaming:
      name: streaming

      max_concurrent_runs: 1

      continuous:
        pause_status: PAUSED
        task_retry_mode: ON_FAILURE

      tasks:
        - task_key: refresh_streaming_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.streaming_etl.id}