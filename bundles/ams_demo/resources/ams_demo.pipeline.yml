resources:
  pipelines:
    ams_demo_etl:
      name: ams_demo_etl

      catalog: ${var.catalog}
      schema: ${var.schema}

      serverless: true

      libraries:
        - glob:
            include: ../../../src/data_platform/lakeflow/ams_demo/transformations/**
