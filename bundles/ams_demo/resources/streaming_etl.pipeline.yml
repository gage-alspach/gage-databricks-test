resources:
  pipelines:
    streaming_etl:
      name: streaming_etl

      catalog: ${var.catalog}
      schema: ${var.schema}

      serverless: true

      libraries:
        - glob:
            include: ../../../src/data_platform/lakeflow/shipment_demo/streaming/**